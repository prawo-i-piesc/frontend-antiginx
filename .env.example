# Example environment variables for frontend-antiginx
# Copy this file to .env.local and customize values for your environment.

# Base URL of the backend API (include protocol and port if needed)
# For local development when backend runs on port 4000 use:
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Example: NEXT_PUBLIC_BACKEND_URL=https://api.example.com
